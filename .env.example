# ===================================================================================
# WHATSAPP SUPPORT (GENFITY-WA-SUPPORT) - ENVIRONMENT CONFIGURATION
# ===================================================================================
#
# USAGE:
#   cp .env.example .env
#   # Edit values, then: docker compose up -d
#
# ===================================================================================

# ==================== PRIMARY DATABASE ====================
# For local dev: use infra-postgres container
# For GCP: use host.docker.internal:6432 (PgBouncer)
DB_HOST=host.docker.internal
DB_PORT=6432
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
DB_NAME=wa_support
DB_SSLMODE=disable

# ==================== TRANSACTIONAL DATABASE ====================
# For local dev: use infra-postgres container
# For GCP: use host.docker.internal:6432 (PgBouncer)
TRANSACTIONAL_DB_HOST=host.docker.internal
TRANSACTIONAL_DB_PORT=6432
TRANSACTIONAL_DB_USER=postgres
TRANSACTIONAL_DB_PASSWORD=your_postgres_password_here
TRANSACTIONAL_DB_NAME=genfity
TRANSACTIONAL_DB_SSLMODE=disable

# ==================== APPLICATION ====================
PORT=8082
GATEWAY_MODE=enabled

# ==================== WHATSAPP API (Backend) ====================
WA_SERVER_URL=http://wa-api:8080
WA_ADMIN_TOKEN=your_wa_admin_token_here

# ==================== AUTHENTICATION ====================
WEBHOOK_VERIFY_TOKEN=your_webhook_verify_token_here
ADMIN_TOKEN=your_admin_token_here
